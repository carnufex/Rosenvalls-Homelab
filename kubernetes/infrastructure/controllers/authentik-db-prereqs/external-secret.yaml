apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-core-secrets
  namespace: authentik
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: authentik-core-secrets
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        AUTHENTIK_SECRET_KEY: "{{ .AUTHENTIK_SECRET_KEY }}"
        AUTHENTIK_REDIS__PASSWORD: "{{ .AUTHENTIK_REDIS_PASSWORD }}"
        AUTHENTIK_BOOTSTRAP_PASSWORD: "{{ .AUTHENTIK_BOOTSTRAP_PASSWORD }}"
        AUTHENTIK_BOOTSTRAP_TOKEN: "{{ .AUTHENTIK_BOOTSTRAP_TOKEN }}"
        AUTHENTIK_BOOTSTRAP_EMAIL: "{{ .AUTHENTIK_BOOTSTRAP_EMAIL }}"
        redis-password: "{{ .AUTHENTIK_REDIS_PASSWORD }}"
  data:
    - secretKey: AUTHENTIK_SECRET_KEY
      remoteRef:
        key: 9af9ea6b-f1ba-4e03-8a5a-b3a00079a73d
        property: password
    - secretKey: AUTHENTIK_REDIS_PASSWORD
      remoteRef:
        key: 9af9ea6b-f1ba-4e03-8a5a-b3a00079a73d
        property: password
    - secretKey: AUTHENTIK_BOOTSTRAP_PASSWORD
      remoteRef:
        key: d557c4c2-29d0-4b9c-bf25-b45100a5f677
    - secretKey: AUTHENTIK_BOOTSTRAP_TOKEN
      remoteRef:
        key: 958d52f8-706f-46fd-8bce-b45100a628c1
    - secretKey: AUTHENTIK_BOOTSTRAP_EMAIL
      remoteRef:
        key: 37c0861a-ba36-4a11-92fb-b45100a64406
