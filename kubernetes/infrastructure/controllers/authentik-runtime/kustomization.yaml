apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: authentik

resources:
  - external-secret-blueprints.yaml
  - httproute.yaml
  - blueprints.yaml

helmCharts:
  - name: authentik
    repo: https://charts.goauthentik.io
    version: 2026.5.6
    releaseName: authentik
    namespace: authentik
    valuesFile: values.yaml
