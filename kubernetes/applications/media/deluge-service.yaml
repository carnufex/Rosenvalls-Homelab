apiVersion: v1
kind: Service
metadata:
  name: deluge
  namespace: media
spec:
  selector:
    app.kubernetes.io/name: deluge-vpn
  ports:
    - name: web
      port: 8112
      targetPort: web
      protocol: TCP
    - name: daemon
      port: 58846
      targetPort: daemon
      protocol: TCP
