apiVersion: v1
kind: Service
metadata:
  name: overseerr
  namespace: media
spec:
  selector:
    app.kubernetes.io/name: overseerr
  ports:
    - name: http
      port: 5055
      targetPort: http
      protocol: TCP
