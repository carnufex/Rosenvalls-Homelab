apiVersion: apps/v1
kind: Deployment
metadata:
  name: matplan-frontend
  namespace: matplan
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: matplan-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: matplan-frontend
    spec:
      serviceAccountName: matplan-runtime
      containers:
        - name: frontend
          image: ghcr.io/carnufex/matplan-frontend@sha256:7df1a57154584b99652ca6dcaa37de1ec3f6df3470e6942ead2031ba5b46dd83
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
