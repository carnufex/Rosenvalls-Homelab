apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matplan-whisper-models
  namespace: matplan
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: longhorn-critical
