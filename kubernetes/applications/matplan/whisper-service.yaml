apiVersion: v1
kind: Service
metadata:
  name: matplan-whisper
  namespace: matplan
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  selector:
    app.kubernetes.io/name: matplan-whisper
  ports:
    - name: http
      port: 8080
      targetPort: http
