apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Onboarded per gitops-app-onboarding: runtime first, public HTTPRoute once the
# image is published and /healthz is green.
resources:
  - ns.yaml
  - image-pull-secret.yaml
  - externalsecret.yaml
  - pvc.yaml
  - service.yaml
  - deployment.yaml
  - httproute.yaml        # gatebound-support.rosenvall.se
