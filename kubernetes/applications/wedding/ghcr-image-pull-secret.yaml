# Private GHCR pull secret for the web image. Uses the shared homelab GHCR PAT
# in Bitwarden (same secret the other apps use — gatebound/matplan/bikepal).
# sync-wave before web so the secret exists when the Deployment pulls.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-image-pull
  namespace: wedding
  annotations:
    argocd.argoproj.io/sync-wave: "-15"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secretsmanager
  target:
    name: ghcr-image-pull
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "ghcr.io": {
                "username": "carnufex",
                "password": "{{ .GHCR_PAT }}",
                "auth": "{{ printf "carnufex:%s" .GHCR_PAT | b64enc }}"
              },
              "registry.rosenvall.se": {
                "username": "homelab",
                "password": "{{ .REGISTRY_PASSWORD }}",
                "auth": "{{ printf "homelab:%s" .REGISTRY_PASSWORD | b64enc }}"
              }
            }
          }
  data:
    - secretKey: GHCR_PAT
      remoteRef:
        key: 09b625aa-6a80-46bb-bf35-b41901624bb3
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: REGISTRY_PASSWORD
      remoteRef:
        key: 227aad11-f6ec-4696-ae94-b47c00a93cd1
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
