apiVersion: apps/v1
kind: Deployment
metadata:
  name: bikepal-frontend
  namespace: bikepal
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bikepal-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bikepal-frontend
    spec:
      serviceAccountName: bikepal-runtime
      containers:
        - name: frontend
          image: ghcr.io/carnufex/bikepal-frontend@sha256:e56e94d3d66f3964ef326b84c4991ecfae054802804659dd5a1a69a4386fa3e9
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
