apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ns.yaml
  - rbac.yaml
  - externalsecret.yaml
  - deployment.yaml

configMapGenerator:
  - name: slack-ops-bot-code
    files:
      - bot.py
    options:
      disableNameSuffixHash: true
